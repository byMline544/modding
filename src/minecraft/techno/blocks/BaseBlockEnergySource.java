package techno.blocks;

public abstract class BaseBlockEnergySource extends BaseBlockMachine {
    public BaseBlockEnergySource(int id, String textureKey, int guiId) {
        super(id, textureKey, guiId);
    }
}
